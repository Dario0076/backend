services:
  - type: web
    name: gestion-pedidos-api
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm run deploy
    envVars:
      - key: NODE_ENV
        value: production
